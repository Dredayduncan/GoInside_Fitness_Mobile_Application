import 'package:flutter/material.dart';

class ThankYouLite extends StatelessWidget {
  const ThankYouLite({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
